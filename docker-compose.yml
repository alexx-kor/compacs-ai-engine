# Main Docker Compose entrypoint.
#
# `include:` requires Docker Compose v2.20+ (you have v2.15.x).
# Service definitions live in the files listed in COMPOSE_FILE (.env).
#
# Manual merge (same as `docker compose up -d` with COMPOSE_FILE set):
#   docker compose -f langfuse-compose.yml -f clickhouse-mcp-compose.yml -f librechat-compose.yml up -d

services: {}
